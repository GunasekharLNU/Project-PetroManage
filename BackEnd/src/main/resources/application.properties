# ===============================
# MySQL Configuration
# ===============================
# Added &allowPublicKeyRetrieval=true to the end of the URL
spring.datasource.url=jdbc:mysql://localhost:3306/petromanage_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Guna2854@

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Explicitly set the dialect to stop the "Unable to determine Dialect" error
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ===============================
# SQL Init
# ===============================
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true